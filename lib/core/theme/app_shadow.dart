import 'package:flutter/material.dart';

class AppShadow {
  AppShadow._();

  static const BoxShadow cardShadow = BoxShadow(
    color: Color(0x0A0F2040),
    blurRadius: 20,
    offset: Offset(0, 4),
  );
}
